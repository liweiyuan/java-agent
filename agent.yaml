# JavaAgent 配置文件

logging:
  # 日志级别: DEBUG, INFO, WARN, ERROR
  level: DEBUG

  console:
    enabled: true

  file:
    enabled: true
    # 日志文件路径（相对于工作目录）
    path: logs/agent.log
    # 单个文件最大大小（MB）
    size-mb: 10
    # 最多保留历史文件数
    max-backups: 5
